pin configuration:
	output pin1 13

do-once:
	if ((fibonacci 15) == 610):
		set pin1 high
	else:
		set pin1 low


fibonacci nth:
	if (nth == 1):
		return 1
	if (nth == 2):
		return 1
	return (fibonacci nth - 1)  + (fibonacci nth - 2)

